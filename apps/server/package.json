{
  "name": "server",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "bun run --hot src/index.ts",
    "start": "bun run dist/index.js"
  },
  "dependencies": {
    "@Teramisu/auth": "workspace:*",
    "@Teramisu/db": "workspace:*",
    "@elysiajs/cors": "^1.3.3",
    "better-auth": "catalog:",
    "dotenv": "catalog:",
    "elysia": "^1.3.21",
    "omise": "^1.1.0",
    "resend": "^6.6.0",
    "zod": "^4.1.11"
  },
  "devDependencies": {
    "typescript": "^5.8.2",
    "@types/bun": "^1.2.6",
    "tsdown": "^0.16.5",
    "@Teramisu/config": "workspace:*"
  }
}
